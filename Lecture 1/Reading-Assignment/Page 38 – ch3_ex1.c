// Cayanan, A.A.A.
// 2022 - 13990

#include <stdio.h>

int main()
{
	// intizialize variables
	int i, j;
	float x, y;

	// assign values to the variables
	i = 10; 
	j = 20;
	x = 43.2892f;
	y = 5527.0f;
	
	// prints the variables
	// %d and %f declares the type of the variable when printed
	// d for integer and f for float
	printf("i = %d, j = %d, x = %f, y = %f\n", i, j, x, y);
	
	return 0;
}